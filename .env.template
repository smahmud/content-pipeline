# Environment Variables Template
# Copy this file to .env and fill in your actual API keys
# The .env file is already in .gitignore and won't be committed

# Anthropic Claude API Key
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# OpenAI API Key (for future use)
OPENAI_API_KEY=your-openai-api-key-here

# AWS Credentials (for Bedrock)
AWS_ACCESS_KEY_ID=your-aws-access-key-here
AWS_SECRET_ACCESS_KEY=your-aws-secret-key-here
AWS_DEFAULT_REGION=us-east-1

# Ollama (local, no key needed)
# OLLAMA_HOST=http://localhost:11434

# =============================================================================
# PRICING CONFIGURATION (Optional - for custom pricing)
# =============================================================================
# Override default pricing for cost estimation
# Useful for enterprise customers with volume discounts or custom pricing agreements

# Transcription Pricing (cost per minute in USD)
# WHISPER_API_COST_PER_MINUTE=0.006        # OpenAI Whisper API (default: 0.006)
# AWS_TRANSCRIBE_COST_PER_MINUTE=0.024     # AWS Transcribe (default: 0.024)

# Note: LLM pricing is more complex (per-model, per-token)
# For custom LLM pricing, use the config.yaml pricing_override field
# See docs/infrastructure-migration-guide.md for examples
